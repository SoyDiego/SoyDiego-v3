import styled from "@emotion/styled"
import { Link } from "gatsby"

export const Container = styled.div`
  width: 100%;
  display: flex;
  justify-content: center;
`
export const LinkSkill = styled(Link)`
  cursor: pointer;
  margin: 1rem 4rem;
`
